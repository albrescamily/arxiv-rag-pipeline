[project]
name = "rag-system"
version = "0.1.0"
description = "arxiv rag system"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "apache-airflow>=3.3.1",
    "feedparser>=6.0.14",
    "requests>=2.34.2",
]
